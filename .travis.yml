language: node_js
sudo: true
dist: trusty
node_js:
  - 10
branches:
  except:
  - /^v[0-9]/
install:
  - yarn install
  - yarn global add codecov
script:
  - yarn bootstrap
  - yarn run test
after_success:
  - codecov
deploy:
  provider: script
  script: yarn run publish
  on:
    branch: master
cache:
  directories:
  - "$HOME/.yarn-cache"
