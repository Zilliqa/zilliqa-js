{
  "packages": ["packages/*"],
  "version": "0.5.1",
  "npmClient": "yarn",
  "useWorkspaces": true,
  "command": {
    "publish": {
      "allowBranch": ["master", "dev"],
      "message": "chore(release): publish %v"
    }
  }
}
